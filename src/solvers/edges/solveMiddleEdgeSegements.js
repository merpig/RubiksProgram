function move(space,depth,side){
    return (space+(depth<10? "0":"") + depth + side);
}

let solveMiddleEdgeSegments = () => {
    let moveString = "";


    return moveString;
}

module.exports = solveMiddleEdgeSegments;