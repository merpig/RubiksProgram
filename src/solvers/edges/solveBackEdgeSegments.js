function move(space,depth,side){
    return (space+(depth<10? "0":"") + depth + side);
}

let solveBackEdgeSegments = () => {
    let moveString = "";


    return moveString;
}

module.exports = solveBackEdgeSegments;